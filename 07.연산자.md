# 7. 연산자
- 연산자란?
	- 하나 이상의 표현식을 대상으로 산술, 할당, 비교, 논리, 타입, 지수 연산 등을 수행해서 하나의 값을 만든다
	- 이때 연산의 대상을 피연산자라고 부른다

```
// 산술 연산자
5 * 4 // 20

// 문자열 연결 연산자
‘My name is ’ + 'Lee' // My name is Lee;

// 할당 연산자
color = 'red' // 'red'

// 비교 연산자
3 > 5 // false

// 논리 연산자
true && false // false

// 타입 연산자
typeof 'Hi' // string
```

## 7.1 산술 연산자

- 산술 연산자란?
	- 피연산자를 대상으로 수학적인 계산을 수행해서 숫자 값을 만든다. 
	- 산술 연산이 불가능한 경우, NaN을 반환한다

### 7.1.1 이항 산술 연산자
- 이항 산술 연산자란?
	- 2개의 피연산자를 산술 연산하여 숫자 값을 만든다
	- ex) +, -, *, /,%

### 7.1.2 단항 산술 연산자
- 단항 산술 연산자는?
	- 1개의 피연산자를 산술 연산하여 숫자 값을 만든다
	- ex) ++, --, +, -

### 7.1.3 문자열 연결 연산자
-  문자열 연결 연산자의 작동 방식은?
	- + 연산자는 피연산자 중 하나라도 문자열인 경우
~~~
// 문자열 연결 연산자
‘1’ + 2 ; // ‘12‘
1 + ‘2’ ; // ‘12’

// 산술 연산자
1 + 2; // 3

// true는 1로 타입 변환된다
1 + true; // 2

// false는 0으로 타입 변환된다.
1 + false; // 1


// null은 0으로 타입 변환된다.
1 + null ; // 1

// undefined는 숫자로 타입 변환되지 않는다.
+undefined; // NaN
1 + undefined; // NaN


~~~
- true false null이 숫자로 바뀌는 현상을 뭐라고 부르는 가?
	- 암묵적 타입 변환, 타입 강제 변화

## 7.2 할당 연산자

- 할당 연산자란?
	- 우항 피연산자의 결과를 좌항에 있는 변수에 할당한다
	- =, +=, -=, /=, %=, `*=` 
- 할당문은 표현식인 문일까, 표현식이 아닌 문일까?
	- 표현식인 문이다 

## 7.3 비교 연산자

- 비교 연산자란?
	- 좌항과 우항의 피연산자를 비교한 다음에 불리언 값으로 반환한다

### 7.3.1 동등/일치 비교 연산자

- 동등 연산자와 일치 연산자의 공통점은?
	- 좌항과 우항의 피연산자가 같은 값으로 평가되는지 비교해서 불리언 값을 반환한다
- 둘의 차이점은?
	- 동등 비교 연산자는 느슨한데 반해서 일치 비교 연산자는 엄격하다
| 비교 연산자 | 의미        | 사례    | 설명                     |
| ----------- | ----------- | ------- | ------------------------ |
| ==          | 동등 비교   | x == y  | x와 y의 값이 같음        |
| ===         | 일치 비교   | x === y | x와 y의 값과 타입이 같음 |
| !=          | 부동등 비교 | x != y  | x와 y의 값이 다름        |
| !==         | 불일치 비교 | x !== y | x와 y의 값과 타입이 다름 | 

```
// 동등 비교는 결과를 예측하기 어렵다

'0' == ''; // false 
0 == ''; // true
0 == '0'; // true
false == 'false'; // false
false == '0'; // true
false == null; // false
false == undefined; // false

```

- 동등 연산자와 일치 연산자 둘 중에 어떤 것을 더 자주 사용하는 가?
	- 동등 연산자는 결과를 예측하기 어렵기에 보통 일치 연산자를 사용한다
- 일치 연산자에서 주의해야할 점은?
	- `NaN === NaN` 의 결과는 false이다. 유일하게 자신과 일치하지 않는 값
	- `0 === -0` 의 결과는 true이다.
- 그러면 NaN을 확인할 때는 어떻게 하는가?
	- 빌트인 함수인 `Number.isNaN()` 을 사용한다.


### 7.3.2 대소 관계 비교 연산자

- 대소 관계 비교 연산자란 무엇인가?
	- 피연산자의 크기를 비교하여 불리언 값을 반환
	- >, <, >=, <=

## 7.4 삼항 조건 연산자

- 삼항 조건 연산자란?
	- 조건식의 평과 결과에 따라서 반환할 값을 결정한다
	- 조건식 ? 조건식이 true일 때 반환할 값 : 조건식이 false일 때 반환할 값
- 삼항 연산자와 if ... else문의 차이점은?
	- if...else문은 표현식이 아닌 문이라 값처럼 사용 불가능
	- 삼항 연산자는 값처럼 사용 가능

## 7.5 논리 연산자

- 논리연산자란?
	- 좌항과 우항의 피연산자를 논리 연산한다
	- ||, &&, !
```
!0; // true
!'Hello' // false 
```

## 7.6 쉼표 연산자
```
var x, y, z;

x = 1, y = 2, z = 3; // 3
```

## 7.7 그룹 연산자
```
10 * 2 + 3; // 23

// 그룹 연산자를 사용하여 우선순위를 조절
10 * (2 + 3); // 50
```

## 7.8 typeof 연산자
```
typeof '' // string
typeof 1 // number
typeof NaN // number
typeof true // boolean
typeof undefined // undefined
typeof Symbol() // symbol
typeof null // object 
typeof [] // object
typeof {} // object
typeof new Date() // object
typeof /test/gi // object
typeof function() {} // function 
```
- null값의 타입 값은 왜 object인가요?
	- 자바스크립트 첫 번째 버전의 버그
	- 기존 코드의 영향을 줄까 봐 수정 못하는 중
	- 따라서 null을 확인할 때는 ===
```
// undeclared 식별자를 선언한 적이 없다.
typeof undeclared; // undefined
```

## 7.9 지수 연산자
- 지수 연산자란?
	- 좌항 피연산자를 밑으로, 우항 피연산자를 지수로 거듭제곱하는 숫자 값
	- ES7부터 도입됐다
	- Math.pow()보다 가독성이 좋다

```
2 ** 2 // 4
2 ** 0 // 1
2 ** -2 // 0.25

//음수를 거듭제곱의 밑으로 사용해 계산하려면 다음과 같이 괄호로 묶어야 한다

-5 ** 2 // error
(-5) ** 2 // 25
```

## 7.10 그 외의 연산자

| 연산자     | 개요                                                        | 참고   |
| ---------- | ----------------------------------------------------------- | ------ |
| ?.         | 옵셔널 체이닝                                               | 9.4.2  |
| ??         | null 병합 연산자                                            | 9.4.3  |
| delete     | 프로퍼티 삭제                                               | 10.8   |
| new        | 생성자 함수를 호출할 때 사용하여 인스턴스를 생성            | 17.2.6 |
| instanceof | 좌변의 객체가 우변의 행성자 함수와 연결된 인스턴스인지 판별 | 19.10  |
| in         | 프로퍼티 존재 확인                                          | 19.13.1       |


## 7.11 연산자의 부수 효과

- 대부분의 연산자는 다른 코드에 영향을 주지 않는다. 다른 코드에 영향을 주는 일부 연산자는 무엇인가?
	- 할당연산자(=), 증가/감소 연산자(++/--), delete 연산자 

## 7.12 연산자의 우선순위
| 우선순위 | 연산자 |
|:---:|---|
| 1 | 그룹 `()` |
| 2 | 멤버(프로퍼티, 메서드) 접근 `.` `[]`<br>`new`<br>함수 호출<br>옵셔널 체이닝 `?.` |
| 3 | 인자가 없는 `new` |
| 4 | 후위증감 연산자 `number++`, `number--` |
| 5 | 논리 not `!`<br>비트 not `~`<br>단항 부호 `+number` `-number`<br>전위 증감 연산자 `++number` `--number`<br>`typeof`, `void`, `delete`, `await` |
| 6 | 거듭제곱 `**` |
| 7 | 곱하기 `*`, 나누기 `/`, 나머지 `%` |
| 8 | 더하기 `+`, 빼기 `-` |
| 9 | 비트 연산 `<<` `>>` `>>>` |
| 10 | 미만 `<`, 이하 `<=`, 초과 `>`, 이상 `>=`<br>`in`<br>`instanceof` |
| 11 | 동등비교 연산자 `==`, 부등 비교 연산자 `!=`, 일치 연산자 `===`, 불일치 연산자 `!==` |
| 12 | 비트 AND `&`, 비트 XOR |
| 13 | 비트 OR |
| 14 | 논리 AND `&&` |
| 15 | 논리 OR |
| 16 | 삼항 조건 연산자 `조건 ? true : false` |
| 17 | 할당 연산자 `=` `+=` `-=`를 포함한 모든 할당 연산자<br>`yield`, `yield*` |
| 18 | 쉼표 `,` |

## 7.13 연산자 결합 순서

