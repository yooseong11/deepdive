# 변수
## 4.1 변수란 무엇인가? 왜 필요한가?

- 변수를 사용하지 않고 값을 재사용하는 게 가능한가?
	- 메모리에 직접 접근하면 가능하지만 매우 위험하다
- 메모리에 직접 접근하는 일이 매우 위험한 이유는?
	- 만약 실수로 운영체제가 사용하고 있는 값을 변경하면 시스템이 멈추는 치명적인 오류가 발생한다
	- 애초에 자바스크립트는 개발자가 직접 메모리를 제어할 수 있게 허용하지 않는다.
- 만약 개발자가 자바스크립트에 직접 메모리를 제어할 수 있다면 생기는 문제점은?
	- 코드가 실행되기 이전에 값이 저장된 메모리 주소를 알 수가 없다. 알려주지도 않는다.
- 왜 메모리 주소를 알 수가 없나요?
	- 값이 저장될 메모리 주소는 코드가 실행될 때 메모리의 상황에 따라 임의로 결정되기 때문에
- 변수란?
	- 하나의 값을 저장하기 위해 확보한 메모리 공간 자체
	- 위 메모리 공간을 식별하기 위해 붙인 이름
	- 값의 위치를 가리키는 상징적인 이름
- 변수가 생겼을 때 개발자에게 이점은?
	- 직접 메모리 주소를 통해 값을 저장하고 참조할 필요가 없고 변수를 통해 안전하게 접근 가능하다
- 할당이란 무엇인가?
	- 변수의 값을 저장하는 것
- 참조란 무엇인가?
	- 변수에 저장된 값을 읽어들이는 것
- 변수 이름의 효과는?
	- 변수에 저장된 값의 의미를 파악할 수 있다.

## 4.2 식별자

- 식별자란?
	- 변수 이름
	- 어던 값과 구별해서 식별할 수 있는 고유한 이름
	- 사람을 이름으로 구별해서 식별하는 것처럼 값도 식별자로 구별해서 식별할 수 있다.
	- 메모리 주소에 붙인 이름
	- 변수, 함수, 클래스
	- 메모리 상 존재하는 어떤 값을 식별할 수 있는 이름
	- 선언에 의해 자바스크립트 엔진에 존재를 알림
- 식별자의 역할은?
	- 메모리 공간에 저장되어 있는 어떤 값을 구별해서 식별해야 한다
	- 어떤 값에 저장되어 있는 메모리 주소를 기억(저장)해야 한다.
- 식별자는 값을 기억하나요?
	- ㄴㄴ, 메모리가 저장된 메모리 주소를 기억합니다

## 4.3 변수 선언

- 변수 선언이란?
	- 변수를 생성하는 것
	- 값을 저장하기 위한 메모리 주소를 확보하고 해당 주소를 연결해서 값을 저장하도록 준비
- 변수 선언 이후에 메모리 공간이 빼앗길 수도 잇나요?
	- 해제되기 전까지는 누구도 확보된 메모리 공간을 사용할 수 없도록 보호됨
- 변수를 선언할 때 키워드는?
	- var, let, const
- 가장 먼저 나온 변수는?
	- var
- const와 let이 나온 이유는?
	- var의 단점을 보안하기 위해서
- var는?
	- 뒤에 오는 변수 이름으로 새로운 변수를 선언할 것을 지시하는 키워드
- var의 독특한 특징은?
	- 변수가 선언되어 확보한 메모리 공간은 비어있을 것이라고 예상하지만, 그곳에는 undefinded라는 값이 암묵적으로 할당되어 초기화됨
	- 즉 2가지 단계가 존재한다. 선언 단계, 초기화 단계.
- var가 초기화 단계를 넣은 이유는?
	- 메모리 공간에 다른 어플리케이션에서 사용한 값(=쓰레기 값)이 남아 있을 수도 있다. 이런 메모리 공간을 가진지 변수를 참조하면 쓰레기 값이 나온다.  
- 선언하지 않은 변수를 사용할 때 나오는 에러는?
	- referenceError
## 4.4 변수 선언의 실행 시점과 변수 호이스팅
~~~
  console.log(score);
  
  var score
~~~
- 위 결과코드 결과로 참조 에러가 아니라 undefinded가 출력되는 이유는?
	- 변수 선언이 런타임이 아니라 그 이전 단계에서 먼저 실행되기 때문에
- 런타임이란?
	- 소스코드가 한 줄씩 순차적으로 실행되는 시점
- 변수 선언이 소스코드 어디에 위치하는지 상관없이 변수를 참조 할 수 있는 이유는?
	- 자바스크립트 엔진은 변수 선언이 소스코드의 어디에 있든 상관없이 다른 코드보다 먼저 실행해서
- 변수 호이스팅이란?
	- 변수 선언문이 코드의 선두로 끌어 올려진 것처럼 동작하는 자바스크립트 고유의 특징
- 변수 선언만 그런가요?
	- ㄴㄴ. var, let, const, function, class 키워드를 사용해서 선언하는 모든 식별자가 호이스팅 된다. 
## 4.5 값의 할당

- 삾을 할당할 때 사용하는 연산자는?
	- 할당 연산자 ‘=’ 

- 값을 할당할 때 주의할 점은?
	- 변수 선언과 값의 할당의 실행 시점이 다르다
	- 변수 선언은 런타임이전, 값의 할당은 런타임에 실행된다.

## 4.6 값의 재할당

- 재할당이란?
	- 이미 값이 할당되어 있는 변수에 새로운 값을 또다시 할당하는 것.
- 변수와 상수의 차이점은?
	- 변수: 값을 재할당 할 수 있는 것
	- 상수: 값을 재할당 할 수 없는 것
- var의 단점은?
	- 재할당 될 때 메모리가 자동 해제되는데, 이 메모리가 언제 헤제될지 예측할 수 없음.

## 4.7 식별자 네이밍 규칙

- 식별자 네이밍 규칙은?
	> 특수문자를 제외한 문자, 숫자, 언더스코어,달러 기호를 포함할 수 있다
	>  단, 식별자는 특수문자를 제외한 문자, 언더스코어 달러 기호로 시작해야 한다. 숫자로 시작하는 것은 허용하지 않는다
	>  예약어는 식별자로 사용할 수 없다
- 예약어란?
	- 프로그래밍 언어에서 사용되고 있거나 사용될 예정인 단어
	- const elt case class...
- 유의할 점은?
	- 쉼표 사용하지 않기: 가독성 나빠짐
	- 한글, 일본어 사용 금지 : 유니코드 문자로 명명된 식별자는 바람직하지 않음
	- 대소문자를 구분함
	- 변수 선언 외에 별도의 주석이 필요하면 변수의 존재 목적을 명확히 드러내지 못하는 것
- 네이밍 컨벤션 4가지
	~~~
	var firstName
	// 카멜 케이스
	
	var firtst_name
	// 스네이크 케이스
	
	var FirstName
	//파스칼케이스
	
	var strFirstName; // type + identifier
	var $elem = document.getElementById('myId') // DOM 노드
	var obserable$ = fromEvent(document, ;click') //RxJS 옵저버블
	~~~
- 사용처
	- 변수 함수 = 카멜케이스
	- 클래스 이름 = 파스칼 케이스