
# 08. 제어문

- 제어문이란?
	- 조건에 따라서 코드 블록을 실행(조건문)할 때나 반복 실행(반복문)할 때 사용한다
	- 코드 실행 순서를 인위적으로 제어


## 8.1 블록문
- 블록문이란?
	- 코드 블록, 블록이라고 부른다
	- 0개 이상의 문을 중괄호로 묶은 것
	- 단독으로 사용 가능하나  제어문이나 함수를 정의할 때 씀

```
// 블록문

{
	var foo = 10;
}

//제어문
var x = 1;
if (x < 10) {
	x++;
}

// 함수 선언문
function sum(a, b){
	return a+b'
}

```


## 8.2 조건문

- 조건문이란?
	- 주어진 조건식의 평과 결과에 따라서 코드 블록의 실행이 결정함
	- 조건식은 불리언 값으로 평가될 수 있는 표현식

### 8.2.1 if...else 문

- 만약 조건식이 불리언 값이 아닌 값이 올 경우에는?
	- 자바스크립트 엔진이 암묵적으로 불리언 값으로 변환시킴

- 삼항 연산자와 if else문을 어떨 때 사용해야 적절한가?
	- 삼항연산자는 조건에 따라 단순히 값을 변수에 할당하는 경우
	- if else는 조건에 따라 실행해야하는 내용이 복잡한 경우

### 8.2.2 switch 문

```
switch (표현식) {
	case 표현식1:
		switch 문의 표현식과 표현식1이 일치하면 실행될 문;
		break;
	case 표현식2:
		switch 문의 표현식과 표현식2이 일치하면 실행될 문;
		break;
	default:
		switch 문의 표현식과 일치하는 case 문이 없을 때 실행될 문.
}
```

- switch 문을 사용하는 경우는?
	- if else 문으로 해결할 수 있다면 무조건 if elsefh
	- switch문이 가독성이 더 좋다면 switch문으로

## 8.3 반복문

- 반복문이란?
	- 조건의 평가 결과가 참인 경우 코드 블록을 실행

### 8.3.1 for 문

### 8.3.2 while 문

### 8.3.3 do ... while 문

## 8.4 break 문

## 8.5 continue 문