
# 06.데이터타입

 - 데이터 타입이란?
	 - 값의 종류
	 - 모든 데이터는 데이터 타입을 가진다
	 - 7개의 종류, 원시 타입과 객체 타입으로 구별 가능
- 원시 타입의 종류는?
	- 숫자
	- 문자열
	- 불리언
	- undefined
	- null
	- 심벌
- 객체 타입의 종류는?
	- 객체 타입 하나. 
	- 예시) 객체, 함수, 배열 등등

## 6.1 숫자 타입

- 자바스크립트 숫자 타입의 특징은?
	- C나 자바는 숫자 타입이 정수랑 실수로 구분해서 int, long, float, double 등등 다양한 타입을 제공
	- 근데 js 숫자 타입이 단 한 개
	- 모든 숫자를 실수로 구분
	- 정수를 표현하기 위한 데이터 타입이 존재하지 않음
	- 따라서 정수로 표시되는 수를 나누더라도 실수가 나옴
~~~
var binary = 0b100001; // 2진수
var octal = 0o101; //8진수
var hex = 0x41; // 16진수

//표기법만 다를 뿐 모두 같은 값이다.
console.log(binary); //65
console.log(octal); //65
console.log(hex); //65
console.log(binary === octal); //true
console.log(octal === hex); //true
 
//숫자 타입은 모두 실수로 처리된다.
console.log(1 === 1.0); // true

~~~
- 숫자타입의 특별한 값 3개는 무엇인가?
	- Infinity: 양의 무한대
	- -Infinity: 음의 무한대
	- NaN: 산술 연산 불가

## 6.2 문자열 타입

- 문자열 타입이란?
  - 텍스트데이터
  - 0개 이상의 16비트 유니코드 문자의 집합으로 전 세계 대부분의 문자를 표현할 수 있다.

- 문자열 표기법은?
  - 작은 따옴표, 큰 따옴표, 백틱
  - 작은 따옴표가 제일 일반적임

- 따옴표로 문자열을 감싸는 이유는?
  - 키워드나 식별자 같은 토큰을 구별하기 위해서

- 자바스크립트 문자열의 특징은?
  - 원시 타입
  - C에서는 배열로 문자열 표현, Java에선 문자열을 객체로 표현

- 문자열이 원시타입이면 가지는 특징은?
  - 변경 불가능한 값

## 6.3 템플릿 리터럴

- 템플릿 리터럴이란?
	- ES6부터 추가한 문자열 표기법. 백틱(‘``’)을 사용함

### 6.3.1 멀티라인 문자열

- 멀티라인 문자열이 무엇인가?
	- 일반 문자열과 달리 이스케이프 시퀀스를 사용하지 않고 줄바꿈이 허용되는 것

### 6.3.2 표현식 삽입

~~~
var first = 'Ung-mo'
var last = 'Lee';

console.log('My name is' + first + ' ' + last + '.');
console.log(`My name is ${first} ${last}`)
둘이 같은 결과 값
~~~

## 6.4 불리언 타입

- 불리언 타입이란?
	- 참과 거짓. true or false

## 6.5 undefined 타입
- undefined 타입의 값은?
	- 오로지 undefined
- undefined 값을 의도적으로 할당해도 되는가?
	- ㄴㄴ, 자바스크립트 엔진이 변수를 초기화하는 용도로 쓰기 때문에 개발자가 임의로 쓰면 혼란을 줄 수있음
- 변수에 값이 없다는 걸 명시하고 싶을 떄 쓰는 값은?
	- null

## 6.6 null 타입
- null 타입의 값은?
	- null이 유일하다
	- 대소문자를 구별하기에 Null과 NULL은 다르다
## 6.7 심벌 타입

~~~
var key = Symbol('key');
console.log(typeof key); //symbol

var obj = {}

// 이름이 충돌할 위험이 없는 유일무이한 값잉ㄴ 심벌을 프로퍼티 키로 사용
obj[key] = 'value';
console.log(obj[key]); // value
~~~
- 심벌 타입이란?
	- ES6에 추가된 값으로 변경 불가능한 원시 값
	- 다른 값과 중복 되지 않는 유일무이한 값


## 6.8 객체 타입

- 원시 타입과 객체 타입으로 분류하는 이유는?
	- 원시 타입과 객체 타입이 근본적으로 달라서 

## 6.9 데이터 타입의 필요성

### 6.9.1 데이터 타입에 의한 메모리 공간의 확보와 참조

- 메모리 공간을 어느 정도 확보하는지 자바스크립트 엔진은 어떻게 아는가?
	- 변수에 할당되는 값의 데이터 타입에 따라서 메모리 공간의 크기가 할당된다

### 6.9.2 데이터 타입에 의한 값의 해석

- 메모리에서 읽어 들인 2진수를 어떻게 해석하는가?
	- ‘0100 0001’이 숫자로 해석하면 65지만 문자열로 해석하면 ‘A'
	- 할당된 타입으로 해석한다
- 데이터 타입의 필요한 이유는 무엇인가?
	1. 갑을 저장할 때 확보해야 하는 메모리 공간의 크기를 결정하기 위해
	2. 값을 참조할 때 한 번에 읽어 들여야 할 메모리 공간의 크기를 결정하기 위해
	3. 메모리에서 읽어 들인 2진수를 어떻게 해석할지 결정하기 위해

## 6.10 동적 타이핑

### 6.10.1 동적 타입 언어와 정적 타입 언어

- 자바스크립트의 모든 값은 데이터 타입을 가진다 그러면 변수는 어떤 데이터 타입일까?
	- 동적 타입
- 동적 타입이란 무엇인가?
	- 변수를 선언이 아닌 할당에 의해 타입이 결정되는 것
	- 그리고 재할당에 의해 변수의 타입이 언제든지 변하는 것
- 정적 타입도 존재하나요?
	- 넵, C언어같은 정적 타입 언어는 변수를 선언할 때 타입도 같이 선언하고 이후에 변경 불가능함

### 6.10.2 동적 타입 언어와 변수

- 동적타입 언어의 장점은?
	- 편리하다
- 동적타입 언어의 단점은?
	- 변수 값이 언제든지 변경되기 때문에 변수 값 추적이 힘들다
	- 숫자 타입 변수일 것이라 예상했지만 문자열 타입 변수일 수도 있다는 뜻
- 요약하자면?
	- 유연성은 높지만 신뢰성이 떨어진다